# name: Web Build + Deployment to GitHub Pages

on:
  push:
    branches:
      - master
      - main
  pull_request:
    branches:
      - master
      - main
  workflow_dispatch:

concurrency:
  group: "pages"
  cancel-in-progress: false

env:
  BUILD_NUMBER: 1
  BUILD_VERSION: 1.0.0
  PYTHON_VERSION: 3.12.2
  FLUTTER_VERSION: 3.22.2
  PROJECT_PATH: my_flutter_app # <-- Variável de ambiente para o caminho do seu projeto

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    # Certifique-se de que a etapa de configuração do Python e de instalação das dependências
    # não precisa de um working-directory, a menos que seus arquivos de requisitos estejam
    # em uma pasta específica.
    - uses: actions/setup-python@v2
      with:
        python-version: ${{ env.PYTHON_VERSION }}
    - run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: ${{ env.FLUTTER_VERSION }}
        channel: stable
        cache: true # Enable caching

    # Adicione working-directory a todas as etapas do Flutter para garantir que os comandos
    # sejam executados no diretório correto do projeto.
    - name: Install Flutter dependencies
      run: flutter pub get
      working-directory: ${{ env.PROJECT_PATH }}

    - name: Run Flutter doctor
      run: flutter doctor
      working-directory: ${{ env.PROJECT_PATH }}
    
    - name: Build Flutter web app
      run: flutter build web --base-url /${GITHUB_REPOSITORY#*/} --route-url-strategy hash
      working-directory: ${{ env.PROJECT_PATH }}

    - name: Upload pages artifact
      uses: actions/upload-pages-artifact@v3
      with:
        name: web-build-artifact
        path: ${{ env.PROJECT_PATH }}/build/web

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/configure-pages@v5
      - if: github.event_name == 'push'
        id: deployment
        uses: actions/deploy-pages@v4.0.5
        with:
          artifact_name: web-build-artifact
